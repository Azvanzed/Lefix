fn u64 other(u64 a1) {
    u8 hi = 32;
    u32 hey = 64;

    @asm("
        mov rax, 6000
        nop
        nop
        pause
    ");

    hey = hi;
    ret hey;
}

fn u64 efi_main(u64 image_handle, u64 st) {
    ret other(image_handle);
}